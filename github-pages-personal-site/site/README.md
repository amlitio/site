# Personal Website — GitHub Pages Starter

A clean, modern, fully responsive personal site for a GC/Construction + Finance + Ops + Tech professional.

## ✨ Features
- Mobile-first, responsive layout
- Sticky header, smooth reveal animations
- Project cards (KD Construction, Adler Hydro Vac, RIDIS, ProjectSense AI)
- Skills, credentials, and contact form (mailto)
- SEO tags + Open Graph + JSON-LD
- Dark/Light theme aware with toggle

## 🚀 Quick Start
1. Create a new GitHub repo named `<your-username>.github.io` (required for user pages), or any repo if you prefer project pages.
2. Upload these files to the repo root (index.html, styles.css, script.js, assets/).
3. For **User Pages** (recommended): visiting `https://<your-username>.github.io` serves `index.html` automatically.
   - For **Project Pages**: enable GitHub Pages in **Settings → Pages → Deploy from branch → root**.
4. Replace placeholder text, images in `assets/` (use your own JPEGs).
5. Update contact email in `index.html` form (search for `mailto:`).

## 🌐 Custom Domain (optional but professional)
- Buy a domain (Namecheap, Cloudflare Registrar).
- In repo → Settings → Pages → Custom domain → add `yourdomain.com`.
- Add DNS CNAME: `your-username.github.io`.
- Enable **Enforce HTTPS**.

## 🧰 Customize
- Colors in `:root` CSS variables.
- Add/remove project cards in the `#projects` section.
- Replace the Open Graph image path and JSON-LD content in `<head>`.

## 🧑‍💻 Framer Upgrade Path
Use this site as your live baseline while we build a Framer version:
- Copy section structure and copywriting into a Framer template.
- Reuse the same project thumbnails and text.
- Point your domain to Framer once ready.

---

Made to pair with your KD + Adler portfolio.